pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Getting code from GitHub'
            }
        }

        stage('Run Tests') {
            steps {
                echo 'Running automation tests'
            }
        }

        stage('Finished') {
            steps {
                echo 'Build completed'
            }
        }
    }
}